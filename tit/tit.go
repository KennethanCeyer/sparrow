package tit

func checkExtension(file string) (err error) {
	// TODO: check file extension, and return error when the extension is not supported
}

func ReadFile(file string, model *interface{}) (err error) {
	// TODO: read and parse the config file and fill the model structure
}
